import PlanningCheckpoint from "@/features/planning/PlanningCheckpoint";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Plan Checkpoint | AgentifyAI",
  description: "Complete one focused diagnostic and record the result.",
};

export default PlanningCheckpoint;

